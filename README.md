# Music Chart by Crawling

    npm install express
    npm install pug
    npm install request
    npm install cheerio
    npm install mongoose


    sudo apt-get install mongodb
    sudo service monogdb start

